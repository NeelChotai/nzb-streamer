// Placeholder for byte range mapping

pub struct ByteRangeMapper;

impl Default for ByteRangeMapper {
    fn default() -> Self {
        Self::new()
    }
}

impl ByteRangeMapper {
    pub fn new() -> Self {
        Self
    }
}