// Placeholder for HTTP server

pub struct HttpServer;

impl Default for HttpServer {
    fn default() -> Self {
        Self::new()
    }
}

impl HttpServer {
    pub fn new() -> Self {
        Self
    }
}
