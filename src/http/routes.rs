// Placeholder for HTTP routes

pub struct ApiRoutes;

impl Default for ApiRoutes {
    fn default() -> Self {
        Self::new()
    }
}

impl ApiRoutes {
    pub fn new() -> Self {
        Self
    }
}